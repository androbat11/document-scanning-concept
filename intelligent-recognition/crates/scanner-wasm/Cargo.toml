[package]
name = "scanner-wasm"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
scanner-pipeline = { path = "../../scanner-pipeline" }
wasm-bindgen = "0.2"
js-sys = "0.3"
